<!DOCTYPE html>
<html lang="pt-br">

<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>To Do List</title>
    <!-- CSS only -->
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.2.0-beta1/dist/css/bootstrap.min.css" rel="stylesheet"
        integrity="sha384-0evHe/X+R7YkIZDRvuzKMRqM+OrBnVFBL6DOitfPri4tjfHxaWutUpFmBp4vmVor" crossorigin="anonymous">
        <!-- icons -->
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.8.2/font/bootstrap-icons.css">
    <link rel="stylesheet" href="css/style.css">
</head>

<body>
    <div class="container">
        <h1 class="text-center" id="h1">Lista de Atividade</h1>
        <h2 class="text-center">
            <%=nome%>
        </h2>
        <a href="/login" class="btn btn-outline-primary">
            <i class="bi bi-box-arrow-left"></i>
        </a>
        <div class="row">
            <div class="col-md order-2">
                <h3 class="text-center">Alterar</h3>
                <form action="/alterar" method="post" class="form-control">
                    <input type="hidden" name="id_u" value="<%=id%>">
                    <input type="hidden" name="id_a" value="<%=alterar._id%>">
                    <input type="hidden" name="nome" value="<%=nome%>">

                    <label for="nome" class="form-control bg-gradient-sopa">Titulo da Atividade</label>
                    <input type="text" name="titulo" class="form-control" value="<%=alterar.titulo%>">

                    <label for="data" class="form-control bg-gradient-sopa">Data de entrega:</label>
                    <input type="date" name="data" class="form-control" value="<%=alterar.data.toISOString().slice(0,10)%>">

                    <label for="disciplina" class="form-control bg-gradient-sopa">Disciplica</label>
                    <select name="disciplina" class="form-control">
                        <option value="0">Selecione...</option>
                        <option value="bio" <%=alterar.disciplina==="bio"?"selected":""%>>Biologia</option>
                        <option value="lp2" <%=alterar.disciplina==="lp2"?"selected":""%>>Linguagem de Programação</option>
                        <option value="bd2" <%=alterar.disciplina==="bd2"?"selected":""%>>Banco de dados</option>
                        <option value="inw" <%=alterar.disciplina==="inw"?"selected":""%>>Internet Web</option>
                    </select>

                    <label for="tipo" class="form-control bg-gradient-sopa">Tipo:</label>
                    <select name="tipo" class="form-control">
                        <option value="0">Selecione...</option>
                        <option value="ac" <%=alterar.tipo==="ac"?"selected":""%>>AC</option>
                        <option value="at" <%=alterar.tipo==="at"?"selected":""%>>AT</option>
                        <option value="rec" <%=alterar.tipo==="red"?"selected":""%>>REC</option>
                        <option value="exame" <%=alterar.tipo==="exame"?"selected":""%>>exame</option>
                    </select>

                    <label for="" class="form-control bg-gradient-sopa">Formato de Entrega</label>
                    <div>
                        <input type="radio" name="entrega" value="Teams" class="form-check-input" value="" required <%=alterar.entrega==="Teams"?"checked":""%>>Tafera No Teams
                        <input type="radio" name="entrega" value="Chat" class="form-check-input" value="" required <%=alterar.entrega==="Chat"?"checked":""%>>Chat No Teams
                        <input type="radio" name="entrega" value="Formulario" class="form-check-input" required <%=alterar.entrega==="Formulario"?"checked":""%>>Forms
                        <input type="radio" name="entrega" value="Email" class="form-check-input" required <%=alterar.entrega==="Email"?"checked":""%>>Email
                        <input type="radio" name="entrega" value="Apresentacao" class="form-check-input"
                            required <%=alterar.entrega==="Apresentacao"?"checked":""%>>Apresentação
                    </div>

                    <label class="form-control bg-gradient-sopa">Orientações</label>
                    <textarea name="orientacoes" class="form-control"><%=alterar.instrucoes%></textarea>
                    <div class="input-group">
                        <input value="Voltar" type="button" class="form-control btn btn-info" onclick="history.back()">
                        <input type="submit" value="Alterar" class="form-control btn btn-warning">
                    </div>
                </form>
            </div>
        </div>
    </div>

</body>

</html>